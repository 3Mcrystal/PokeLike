<UserControl x:Class="PokeLike.MVVM.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:VM="clr-namespace:PokeLike.MVVM.ViewModel"

             Height="300" Width="400">
    <UserControl.DataContext>
        <VM:LoginVM />
    </UserControl.DataContext>

    <Grid>
        <!-- Username Field -->
        <TextBox x:Name="UsernameTextBox" Margin="10,10,10,0" VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="30" Text="{Binding Username}" />
        <TextBlock Text="Username" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,15,0,0" Foreground="Gray"/>

        <!-- Password Field -->
        <PasswordBox Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="10,50,10,0" VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="30"/>
        <TextBlock Text="Password" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,55,0,0" Foreground="Gray"/>

        <!-- Login Button -->
        <Button Content="Login" Margin="10,100,10,0" VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="30" Command="{Binding LoginCommand}"/>

        <!-- Error Message -->
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="10,150,10,0" VerticalAlignment="Top" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>